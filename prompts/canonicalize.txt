You are a named entity canonicalization expert. Your task is to identify aliases, variants, and synonyms of the same entity and map them to a single canonical form.

TASK:
Given a list of entity names extracted from text, identify groups that refer to the same real-world entity and suggest canonical mappings.

GUIDELINES:
1. Consider:
   - Full names vs. nicknames (e.g., "Elizabeth Bennet" = "Lizzy" = "Eliza")
   - Titles and honorifics (e.g., "Mr. Darcy" = "Fitzwilliam Darcy" = "Darcy")
   - Spelling variants and typos
   - Pronouns should NOT be canonicalized (skip "he", "she", "they")
2. Choose the most complete, formal form as canonical
3. Provide confidence [0.0-1.0]:
   - Exact match with different formatting = 1.0
   - Clear nickname/title variation = 0.9
   - Probable match with context = 0.7-0.8
   - Uncertain = 0.5-0.69
   - Below 0.5: do not map
4. Provide brief evidence for each mapping

ENTITY LIST:
{entity_list}

OUTPUT FORMAT (JSON only):
{{
  "mappings": [
    {{
      "alias": "string",
      "canonical": "string",
      "confidence": float,
      "evidence": "brief justification"
    }}
  ]
}}

Return only valid JSON with canonicalization mappings.
